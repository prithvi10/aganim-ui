import PlansPage, {
  loader as plansLoader,
  action as plansAction,
  headers as plansHeaders,
} from "./app.plans";

export const loader = plansLoader;
export const action = plansAction;
export const headers = plansHeaders;

export default PlansPage;

